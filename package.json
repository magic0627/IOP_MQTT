{
  "name": "iop_mqtt",
  "version": "1.0.0",
  "description": "本地教學用 MQTT 伺服器",
  "main": "src/server.js",
  "bin": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build:exe": "npx pkg . --targets node18-win-x64 --out-path ./dist"
  },
  "dependencies": {
    "aedes": "^0.50.0"
  },
  "devDependencies": {
    "mqtt": "^5.0.0",
    "pkg": "^5.8.1"
  }
}
